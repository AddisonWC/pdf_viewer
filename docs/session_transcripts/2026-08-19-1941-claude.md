# 2026-08-19 19:41 — Claude Code

- Source: `Claude Code`
- Session: `39f0ff1d-1664-4b4f-ba0e-09ab1a49c428`
- Span: 2026-08-19 19:41:39 PDT → 2026-08-19 20:36:39 PDT
- User messages: 8

## User — 2026-08-19 19:41:39 PDT

/effort xhigh

## User — 2026-08-19 19:43:23 PDT

Take a look at the transcripts from the previous two agents. Given that the last agent felt it was appropriate to add anything to HISTORY.md, I feel like METADOCS.md is poorly written. What do you think I should change? Shorter and clearer is better.

## Assistant (final reply) — 2026-08-19 19:46:40 PDT

METADOCS is loaded every session and the agent still wrote those two entries, so the file did license them. Here's where.

## Why it licensed the entry

**The HISTORY test excludes nothing.** "Belongs here only if not knowing it would lead an agent to undo a deliberate decision" is true of *every* deliberate decision — that's what makes it deliberate. Both entries pass it cleanly. A filter that never rejects reads as permission.

**"How the program reached its present shape" invites a changelog.** Every landed change is part of how it got here. Combined with the test above, "we changed X, Addison asked for it" is the obvious output.

**"When to update what" is four independent triggers with no default and no priority.** A session that changed behavior, closed a question, and finished work matches three of them. Both facts duly went into ARCHITECTURE *and* DEVLOG *and* HISTORY — the same redundancy you caught on XWayland last session.

**Nothing says a fact has one home.** You enforced that rule on the text by hand; it never got written into the rules.

The strict bar you want is already in the file — but only for bugs, buried at the bottom under "Comments versus docs": *"and only if the bug still constrains something today."* That's the idea that needs to be HISTORY's definition.

## What I'd change

**1. Give HISTORY a test the code itself can fail.**

> **HISTORY.md** — only what the working code cannot show: approaches tried and abandoned, alternatives deliberately rejected, bugs that still constrain the design. If an agent could learn it from the code and `ARCHITECTURE.md`, it does not belong here. A change that landed and worked is not history.

Under that rule the clipboard change fails on the first sentence (it worked) and again on the second (ARCHITECTURE §"Selection and the two clipboards" already carries your reason).

**2. Replace "When to update what" with routing that has a default.**

> **Where a fact goes**
>
> Every session: whatever changed how the program works → `ARCHITECTURE.md`, same edit; the session's work → an entry in `DEVLOG.md`; any key that changed → `HELP_HTML`, which is hand-written and drifts silently.
>
> Rarely: `GOTCHAS.md`, when library or platform behavior cost you debugging time. `HISTORY.md`, when a later agent would otherwise retry something that already failed, or revert something for the reason it was already rejected — and under **Settled**, only a question with a live alternative someone could argue for. Working code with its rationale in `ARCHITECTURE.md` needs nothing in HISTORY.

"Every session / rarely" is the part that was missing. The **Settled** clause also matters: XWayland and the Ask-agent belong there because someone would genuinely argue the other side. Which side the busy indicator sits on has no rival candidate — it's just how it works, which is ARCHITECTURE.

**3. Add one line to "What earns a line."**

> Each fact has one home. Cross-reference rather than restate: the same point in two files is a line paid for twice and two things to keep true.

Trimming the attribution section (folding the two code blocks into one contrast, seven bullets to six) takes the file from 112 lines to 101 with the new section included. Full draft: `scratchpad/METADOCS-proposed.md`.

## Two follow-ons

HISTORY.md's own header repeats the loose framing — "how the program reached its present shape, and which questions are closed" — so an agent that opens the file gets the old license back even after METADOCS is fixed. It should state the same test.

And the two entries from last session should come out: both duplicate ARCHITECTURE, and leaving them in is a worked example of the behavior you're trying to stop.

Want me to apply all of this?

## User — 2026-08-19 19:51:02 PDT

It's really unpleasant for me to retrieve your scratchpad location. Put the draft in the project directory; I'll delete it myself after I've read it. I'll ask you to apply that stuff after I see what you've written.

## Assistant (final reply) — 2026-08-19 19:51:21 PDT

`METADOCS-proposed.md` is in the project root — a clean drop-in replacement for `docs/METADOCS.md`, 101 lines against the current 112. Nothing else is touched.

Changes in it, all in the first half:

- **HISTORY.md blurb** rewritten around a test the code can fail.
- **"What earns a line"** gains the one-home/cross-reference line.
- **"When to update what"** → **"Where a fact goes"**, split into every-session and rarely.
- **The files** intro and the Addison's-files line merged into one paragraph.
- **Attribution** trimmed: the two code blocks folded into one contrast, seven bullets to six. No rule dropped — the section's content is the part you already worked over, so I only compressed it.

Not in the file, since it's a different doc — the matching header for `docs/HISTORY.md`:

```
# History

Only what the working code cannot show: approaches tried and abandoned,
alternatives deliberately rejected, bugs that still constrain the design. If it
can be learned from the code and `ARCHITECTURE.md`, it doesn't go here.
The per-session record is `DEVLOG.md`.
```

That replaces "How the program reached its present shape, and which questions are closed. Short on purpose." — the phrasing that hands back the license after METADOCS is fixed.

## User — 2026-08-19 19:57:58 PDT

this looks a lot better. I changed something, but I forgot what it was (sorry). Make drafts of modifications to HISTORY.md and ARCHITECTURE.md as well.

## Assistant (final reply) — 2026-08-19 20:00:34 PDT

Both drafts are in the project root: `HISTORY-proposed.md` (78 → 61 lines) and `ARCHITECTURE-proposed.md` (256 → 257).

**ARCHITECTURE — two edits.** The header said "Why it ended up this way is in `HISTORY.md`", which sends the reader away for rationale the new split puts here; it now reads "How `pdfviewer.py` works now, and why. Approaches that were tried and abandoned are in `HISTORY.md`." And §Overlays picks up the orphaned-keys fact — Reset-Zoom and Zoom-to-Content survive as keys with no button — because that's current state, not history.

**HISTORY — cuts.** Both August 19 entries go, and neither needed anything moved into ARCHITECTURE first: the spinner position and the two-clipboard rule were already there in full, including your attribution. That's the evidence they were duplicates rather than history. "Documentation moved into the repo" also goes, with its one durable clause folded into the Gap note — that the account was assembled from one agent's memory on one machine, which is why it's thin and single-model-shaped.

Three judgment calls worth your eye:

- **"Text selection was substantially harder than anticipated"** — cut. It names no failed approach and no trap, and GOTCHAS has no selection entries, so there's nothing an agent can act on. You said it originally about why the raw reference entry goes to Scholar unparsed, which ARCHITECTURE already carries. If you want the warning, it needs the specific thing that went wrong.
- **"Adding figures made cropping slow"** — kept, but cut to one sentence. The closest call in the file: ARCHITECTURE explains the mechanism (tens of thousands of marks, O(n²) clustering) and GOTCHAS explains `get_bboxlog()`'s unbounded cost, so by a strict reading the entry is already covered. I kept it because those two say the code *is* vectorized and budgeted, not that the naive version was actually unusable.
- **"Content detection was built in three passes"** — reframed. The list of three sources is ARCHITECTURE's; what survives here is the two failures that forced passes 2 and 3, since the plot-cropped-to-its-caption case appears nowhere else.

The rest is trimming where a fact had two homes: the RAM entry keeps the bug report that triggered it and drops its restatement of ARCHITECTURE's buffering constraints; the toolbar entry keeps only the rejection; the dead "module docstring still said minimal" trivia goes.

## User — 2026-08-19 20:06:17 PDT

Looks good. I'm not really sure about the amount of content in ARCHITECTURE at present. a lot of it seems like stuff not every agent needs to know; they could probably look it up as-relevant. Do you think we can change it to be more like that? Do you have any ideas as to how we should do that?

## Assistant (final reply) — 2026-08-19 20:07:32 PDT

Yes. The file mixes two things: constraints that bind an agent working anywhere, and detail that only matters if you're editing that subsystem. The second kind is most of it.

**Split test:** would an agent working on a *different* part get it wrong without this? If yes it stays in ARCHITECTURE; if no it's lookup.

Applied to the current sections:

- **Stays** — Design principles, Platform, the one-module note, plus the cross-cutting invariants currently buried inside subsystem sections: every background task carries a generation token and drops superseded results, overlays are never in a layout, the render thread count is deliberately low and adding threads starves the GUI. Those get broken from outside by an agent who never opened the relevant section.
- **Moves** — Document source, Rendering, Content detection, Overlays, Selection, Search, TOC, Zoom/gestures, Citations, Ask agent, History/copies/persistence, Testing. ~186 of 256 lines. `_detect_pending_gen` being an int, HSL dark-mode inversion, the `/proc/self/fd/N` fallback, `_REF_MARKER` column clustering — none of that changes what an agent touching search would do.

**How.** One new `docs/SUBSYSTEMS.md` holding those sections roughly verbatim, grepped like DEVLOG and GOTCHAS. ARCHITECTURE keeps the principles and gains a map — one line per subsystem naming its entry points so the grep target is right there:

```
- **Content detection** — `detect_content_rect`, `_ContentDetectTask`, `C`/`Shift+C`.
  Text, graphics and raster passes, both time-budgeted. → `SUBSYSTEMS.md`
```

That lands ARCHITECTURE around 65–75 lines. The always-loaded set — METADOCS + ARCHITECTURE + HISTORY — is 446 lines today, 419 with the drafts you've read, and about 230 after this. Roughly half.

Two things that make it hold up:

**METADOCS needs one line**, or the split recreates the routing ambiguity we just removed from HISTORY: *"SUBSYSTEMS.md — how one area works in detail, opened when you're changing that area. A fact belongs in ARCHITECTURE only if an agent working on a different part would get it wrong without it."*

**AGENTS.md doesn't need editing.** It points at three files; ARCHITECTURE's map does the routing to the fourth. Your file stays as it is.

One alternative I'd only use in part: pushing the detail into code comments instead of a second doc. It's right for a handful of items that are the *why* of exactly one function — `_detect_pending_gen` being an int is the clearest — and I'd move those to the code rather than to SUBSYSTEMS. Wholesale it doesn't work: a comment is only found by an agent already in the right place, and the module is 4k lines.

Worth flagging before I draft: several moved sections carry your specs, the TOC paging semantics most of all. Moving them out of the always-loaded file doesn't weaken the attribution, but it does mean an agent only meets them once it's in that area.

I'd also dedupe against GOTCHAS as I go — thread-safety and overlays-in-a-layout are currently stated in both.

Want me to draft `ARCHITECTURE-proposed.md` and `SUBSYSTEMS-proposed.md` this way?

## User — 2026-08-19 20:10:22 PDT

To do this effectively with the edits to code comments you need to edit the pdfviewer.py as well. You can edit that and put your new versions directly into /docs, no drafts required.

## Assistant (final reply) — 2026-08-19 20:18:42 PDT

Done. Tests pass at 34 groups / 175 checks.

**Docs**
- `docs/ARCHITECTURE.md` — 256 → 104 lines: your three design principles, the invariants that get broken from outside (generation tokens, overlays never in a layout, one render thread, render-at-display-size, best-effort persistence), the platform default, a symbol index, and how to run the suite.
- `docs/SUBSYSTEMS.md` — new, 120 lines. One section per area: its shape, which parts are your requirements, known gaps. It points at the comments instead of restating them.
- `docs/METADOCS.md` — SUBSYSTEMS added, and "Where a fact goes" now heads its list with the comment at the code, since that's the routing the split depends on.
- `docs/HISTORY.md`, `docs/DEVLOG.md` — the drafts you approved, plus DEVLOG's header (it still routed safety-critical facts to ARCHITECTURE/HISTORY) and this session's entry.

The always-loaded set is 446 → 280 lines. I'd guessed ~230; ARCHITECTURE landed at 104 rather than 70, mostly in the index and the principles, which I didn't want to compress further.

**Code.** The split turned out to be mostly deletion rather than moving. Your comments already explain nearly everything ARCHITECTURE's subsystem sections said — the `/proc/self/fd` fallback, the HSL dark-mode derivation, the detection budgets, `_detect_pending_gen`, the reference-extraction failure modes, the TOC spec — usually in more detail than the doc. The only content with no home at the code was the Ask agent's two gaps, now comments at `_launch_claude_helper` and its call site: the hard-wired terminal/model flags against `FEATURES.md`'s stated intent, and the stale path after a move.

**One thing I found.** `HELP_HTML` still told the reader the busy indicator was "a small dot to its left." That's the change you asked for last session — the help text was updated for Ctrl+C but not for the indicator, and two comments still called it a dot. All three corrected; noted in DEVLOG.

Two things left for you: `AGENTS.md` describes ARCHITECTURE as "current structure and design of the program," which is now the rules and the index — reword if you want, and it's your call whether it should name SUBSYSTEMS at all, since ARCHITECTURE routes there anyway. And the three `*-proposed.md` files in the root are superseded; say the word and I'll delete them, or leave them to you.

## User — 2026-08-19 20:33:13 PDT

What is with the subheadings in each of the other markdown files? do we need them? Much of their content seems redundant with METADOCS.md. It seems to me like ARCHITECTURE.md basically still follows the description given in AGENTS.md so I don't see why we need to change it. If we delete the subheadings we should say something about what pdfviewer.py is outside of the subheading.

## Assistant (final reply) — 2026-08-19 20:34:45 PDT

Done, docs only — the code edits were already covered by the passing run.

Every doc's opening paragraph is gone. HISTORY's was the worst case: I'd written it by copying METADOCS' blurb sentence for sentence. `SUBSYSTEMS.md` now goes straight from its title into `## Document source`.

What I kept, because METADOCS doesn't say it:

- **DEVLOG** — the machine caveat about the numbers, which METADOCS points at by name.
- **GOTCHAS** — "check this before concluding a handler is broken," with the evidence that several "the key does nothing" reports were binding-layer bugs. That's a trigger for opening the file, not a description of it.
- **ARCHITECTURE** — replaced with what you asked for, a sentence on the program rather than on the document: `pdfviewer.py` is the whole thing, an M×N grid that slides one page at a time on PySide6 and PyMuPDF, one ~4k-line module by accretion.

METADOCS gained one clause so this doesn't grow back: *"That includes what a file is for — it's described above, so no file introduces itself."*

Always-loaded set is now 272 lines against 446 when we started. AGENTS.md stays as it is.

## User — 2026-08-19 20:36:05 PDT

Sounds good, prepare for /clear

## Assistant (final reply) — 2026-08-19 20:36:39 PDT

Ready to `/clear`.

**State:** docs restructured, `pdfviewer.py` edited, suite passing at 34 groups / 175 checks (last run was after the final code edit; everything since has been docs). No memories written.

**Written down for the next agent:**
- `docs/METADOCS.md` — HISTORY's new test, the one-home rule, routing headed by the comment at the code, the SUBSYSTEMS entry, and the no-file-introduces-itself clause.
- `docs/ARCHITECTURE.md` (100 lines) and `docs/SUBSYSTEMS.md` (new, 115) — the split.
- `docs/HISTORY.md` (56) — trimmed, with the two duplicate August 19 entries gone.
- `docs/DEVLOG.md` — this session's entry, with final line counts and the `HELP_HTML` correction.
- `pdfviewer.py` — the Ask agent's two gaps as comments; help text and two comments no longer call the busy indicator a dot.

**Loose ends:**
- Three stale drafts sit in the project root — `METADOCS-proposed.md`, `HISTORY-proposed.md`, `ARCHITECTURE-proposed.md`. All three are superseded by what's in `docs/`. You said you'd delete them yourself, so I've left them, but a fresh agent will find them and they no longer match the real files.
- Carried over from last session and still true: the spinner has never been seen on a real display. I've now edited the help text that describes it, also unverified by eye.
- `docs/REVIEW-2026-08.md` is untouched, per your instruction not to work on it.
