# 2026-08-19 19:31 — Claude Code

- Source: `Claude Code`
- Session: `5f7bf6fe-6a16-4df0-8e32-e81bb9beba4e`
- Span: 2026-08-19 19:31:55 PDT → 2026-08-19 19:41:01 PDT
- User messages: 3

## User — 2026-08-19 19:31:55 PDT

/effort med

## User — 2026-08-19 19:32:10 PDT

take a look at designer_notes.txt

## Assistant (final reply) — 2026-08-19 19:38:26 PDT

Both changes from `designer_notes.txt` are in, with docs and tests updated.

**Busy indicator** (`pdfviewer.py`)
- The leading blue dot became a spinner appended *after* the page range, so the numbers never shift when background work starts or stops (`_render_status`).
- `BUSY_FRAMES` is four quadrant-filled circles (◐◓◑◒) — same advance width, so the card doesn't twitch — stepped by `_busy_timer` every `BUSY_INTERVAL_MS = 500`. The timer only runs while `_loading`, so an idle window isn't repainting twice a second, and each new burst of work restarts at frame 0.

**Copying** (`pdfviewer.py`)
- `on_selection_changed` now writes only PRIMARY (`QClipboard.Selection`) — the "what's selected right now" clipboard that middle-click pastes.
- New `Ctrl+C` → `copy_selection` writes CLIPBOARD. So reading a paper no longer clobbers whatever you copied elsewhere.
- One wrinkle handled: a window-level `QShortcut` fires even while the search/goto/Ask field has focus, which would have stolen that field's own Ctrl+C. `copy_selection` defers to a focused `QLineEdit` that has a selection.
- Ctrl+H help text updated for both (`HELP_HTML`).

**Tests** — 32 groups / 163 checks → 34 / 175, all passing in ~21s. Both new groups were confirmed to fail against the pre-change logic, per the rule in `GOTCHAS.md`. One note: the offscreen platform reports `supportsSelection() == False`, so the PRIMARY-content assertion is skipped there; the load-bearing check is that a drag leaves CLIPBOARD untouched. `test_status_format` switched to `startswith` since the indicator is now a suffix.

**Docs** — `ARCHITECTURE.md` (overlays section rewritten, new "Selection and the two clipboards" section), `HISTORY.md` (why selection stopped writing CLIPBOARD; indicator position added to "settled"), `DEVLOG.md` (session entry).

One thing I haven't done is look at it running — the spinner glyphs are only verified by their text content in the offscreen suite, not by eye. Worth a glance next time you open a big scan, where the load is long enough to watch it turn.

## User — 2026-08-19 19:40:53 PDT

Whatever, prepare for /clear

## Assistant (final reply) — 2026-08-19 19:41:01 PDT

Nothing pending — the session's record is already where a fresh agent will find it.

**State:** both items from `designer_notes.txt` are done, tests pass (34 groups / 175 checks), and no scratch files were left in the project.

**Written down for the next agent:**
- `docs/ARCHITECTURE.md` — overlays section (spinner, `BUSY_FRAMES`/`_busy_timer`) and a new "Selection and the two clipboards" section.
- `docs/HISTORY.md` — why selection stopped writing CLIPBOARD; indicator position added under "Settled — don't reopen".
- `docs/DEVLOG.md` — session entry, including the offscreen `supportsSelection()` limitation.
- `HELP_HTML` in `pdfviewer.py` — Ctrl+C and the primary-selection behavior.

**Only loose end:** neither change has been seen running on a real display; the spinner is verified by text content offscreen, not by eye.

Safe to `/clear`.
