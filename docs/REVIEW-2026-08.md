# Outside code review by sol — 2026-08-17

An independent review of `pdfviewer.py` by sol, recorded as a dated artifact to
diff future reviews against. Findings are marked as they are dealt with; the
rest are open. Line numbers are from that date and have drifted.

Its summary: *"an unusually thoughtful personal PDF viewer. Rendering, mixed-DPI
handling, cropping, navigation, and regression coverage show substantial
real-world tuning… It feels more like a mature, performance-tuned power-user
application than the 'minimal viewer' its description suggests."* All 32 headless
test groups passed in ~21 s; compilation succeeded.

## Findings

(all closed)

## Broader suggestions

- Version ranges or a lock file — `requirements.txt` is completely unpinned
  (`PySide6`, `PyMuPDF`, `numpy`, no constraints at all).
- Split the module into rendering/document, crop detection, search, integrations,
  and UI.
- Convert the test harness to pytest; add cases for async cancellation, encrypted
  PDFs, platform selection, and copy-window zoom state.
- **Make state writes atomic** — the direct JSON overwrite at `pdfviewer.py:238`
  can lose every saved position if interrupted.
